import { StyleSheet } from "react-native";
import {COLORS} from "../constents/index";

const styles = StyleSheet.create({
    drawerStyle: {
        backgroundColor: COLORS.white,
        width: 250
    },
    drawerLabelStyle: {
        color: COLORS.gray
    }
})

export default styles